machine:
  services:
    - docker

dependencies:
  post:
    - docker build -t 463442625423.dkr.ecr.us-east-2.amazonaws.com/yyyyello-team:$CIRCLE_SHA1 .

test:
  post:
    - docker run -d -p 8080:8080 --name yyyyello-team 463442625423.dkr.ecr.us-east-2.amazonaws.com/yyyyello-team:$CIRCLE_SHA1; sleep 10
    - curl --retry 10 --retry-delay 5 18.218.194.15:8080 | grep "Hello World!"

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
